import { TestClient } from '@/components/TestClient';
export default async function StartPage({ searchParams }: { searchParams: Promise<Record<string, string | string[] | undefined>> }) { const params = await searchParams; const seed = typeof params.seed === 'string' ? params.seed : undefined; return <main><header className="page-header"><p className="eyebrow">Start test</p><h1>6 prompts. 6.7-ish seconds. Don’t embarrass yourself.</h1><p className="muted">Tap through the nonsense and we’ll translate your timing into a 67 Speed score.</p></header><TestClient seed={seed} /></main>; }
